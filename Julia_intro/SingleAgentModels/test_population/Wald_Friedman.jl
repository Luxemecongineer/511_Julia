#Sen Lu. Example comes from QuantEcon.

"""

One should agree that J should satisfy the bellman equation.
J(p)= min{(1-p)*L0, p*L1, c + E[J(p')]}
where p' is the random variable defined by a bayesion process.
"""
